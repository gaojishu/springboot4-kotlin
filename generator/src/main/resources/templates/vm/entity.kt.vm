package ${package.Entity}

#foreach($pkg in ${table.importPackages})
import ${pkg}
#end

@TableName("${table.name}")
data class ${entity}(
    #foreach($field in ${table.fields})
        var ${field.propertyName}: ${field.propertyType}? = null#if($foreach.hasNext),#end
    #end
) : ${superEntityClass}()
